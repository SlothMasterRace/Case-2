general:
  build_dir: /src/test/
checkout:
  post:
    - git submodule sync
    - git submodule update --init
test:
  override:
    - php ./app/run-server.php --daemon
    - php ./test-suite/run.php --unit-tests
